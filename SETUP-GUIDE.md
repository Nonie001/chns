# คำแนะนำการใช้งานระบบออกใบเสร็จการบริจาค

## 📝 ขั้นตอนการตั้งค่าครั้งแรก

### 1. ติดตั้งโปรเจค
```bash
npm install
```

**หมายเหตุ:** โปรเจคนี้ใช้ Puppeteer สำหรับสร้าง PDF แบบ server-side rendering

### 2. ตั้งค่า Supabase

#### 2.1 สร้างโปรเจคใน Supabase
- ไปที่ https://supabase.com
- คลิก "New Project"
- กรอกชื่อโปรเจคและรหัสผ่าน database
- รอโปรเจคสร้างเสร็จ (ประมาณ 2-3 นาที)

#### 2.2 รัน SQL Script
- ไปที่ **SQL Editor** ในเมนูซ้าย
- คลิก "New Query"
- คัดลอกเนื้อหาจากไฟล์ `supabase-setup.sql`
- วาง (Paste) ในช่อง SQL Editor
- คลิก "Run" เพื่อรัน script

#### 2.3 สร้าง Storage Bucket
- ไปที่ **Storage** ในเมนูซ้าย
- คลิก "Create a new bucket"
- ตั้งชื่อ bucket: `donations`
- เลือก "Public bucket"
- คลิก "Create bucket"

#### 2.4 ตั้งค่า Storage Policies
- คลิกที่ bucket `donations` ที่สร้างไว้
- ไปที่แท็บ "Policies"
- คลิก "New Policy"
- เลือก "For full customization" แล้วใช้ SQL จาก `supabase-setup.sql` (ส่วน Storage Bucket Policies)

#### 2.5 คัดลอก API Keys
- ไปที่ **Project Settings** > **API**
- คัดลอกค่าต่อไปนี้:
  - Project URL
  - anon public key
  - service_role secret key

### 3. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ในโฟลเดอร์หลัก:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin Login (เปลี่ยนเป็นรหัสที่ปลอดภัย)
NEXT_PUBLIC_ADMIN_USERNAME=admin
NEXT_PUBLIC_ADMIN_PASSWORD=YourSecurePassword123
```

**หมายเหตุ:** Email settings สามารถตั้งค่าผ่าน Admin Settings UI ได้ ไม่จำเป็นต้องใส่ใน env

### 4. ตั้งค่า Gmail SMTP (สำหรับส่งอีเมล)

#### 4.1 เปิด 2-Step Verification
- ไปที่ https://myaccount.google.com/security
- เลื่อนลงมาที่ "2-Step Verification"
- คลิก "Get started" และทำตามขั้นตอน

#### 4.2 สร้าง App Password
- ไปที่ https://myaccount.google.com/apppasswords
- เลือก "App": Mail
- เลือก "Device": Other (Custom name)
- ตั้งชื่อ: "Donation Receipt System"
- คลิก "Generate"
- **คัดลอกและเก็บ password 16 หลักนี้ไว้** (จะแสดงครั้งเดียว)

### 5. เริ่มใช้งาน

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ http://localhost:3000

---

## 🎯 วิธีใช้งานระบบ

### สำหรับ Admin (ครั้งแรก)

#### ขั้นตอนที่ 0: Login
1. เปิด http://localhost:3000/login
2. ใส่ Username และ Password ที่ตั้งไว้ใน `.env.local` (default: admin/admin123)
3. คลิก "เข้าสู่ระบบ"

#### ขั้นตอนที่ 1: ตั้งค่า Email
1. คลิกปุ่ม "ตั้งค่า" มุมขวาบน
2. กรอกข้อมูลดังนี้:

**ข้อมูลผู้ส่ง:**
- ชื่อผู้ส่ง: `มูลนิธิการกุศล` (หรือชื่อองค์กรของคุณ)
- Email ผู้ส่ง: `your-email@gmail.com`

**การตั้งค่า SMTP:**
- SMTP Host: `smtp.gmail.com`
- SMTP Port: `587`
- SMTP Username: `your-email@gmail.com`
- SMTP Password: `app-password-16-หลักที่สร้างไว้`

5. (Optional) อัพโหลดรูปลายเซ็นและกรอกชื่อ-ตำแหน่งผู้ลงนาม
6. คลิก "บันทึกการตั้งค่า"

#### ขั้นตอนที่ 2: ตรวจสอบและอนุมัติการบริจาค
1. กลับไปที่หน้า Admin Dashboard (http://localhost:3000/admin)
2. คุณจะเห็นรายการบริจาคที่รอตรวจสอบ
3. คลิกที่ไอคอน "ตา" เพื่อดูรายละเอียดในหน้าใหม่
4. ตรวจสอบข้อมูลและหลักฐานการบริจาค
5. คลิก "อนุมัติและส่งใบเสร็จ"
   - ระบบจะสร้างใบเสร็จ PDF อัตโนมัติ (รองรับภาษาไทย, แสดงจำนวนเงินเป็นตัวอักษร)
   - ส่งอีเมลพร้อมใบเสร็จไปยังผู้บริจาค
   - PDF มีลายเซ็นถ้าตั้งค่าไว้
6. สถานะจะเปลี่ยนเป็น "อนุมัติแล้ว"
7. สามารถพรีวิว และดาวน์โหลด PDF ใหม่ได้ทุกเมื่อจากปุ่มที่รายการ

### สำหรับผู้บริจาค

1. เปิด http://localhost:3000 (หน้าหลัก)
2. กรอกข้อมูลในฟอร์ม:
   - คำนำหน้า (นาย/นาง/นางสาว)
   - ชื่อ-นามสกุล
   - Email (ใช้สำหรับรับใบเสร็จ)
   - วันเกิด
   - เบอร์โทรศัพท์
   - จำนวนเงินที่บริจาค
4. คลิก "อัพโหลดไฟล์" เพื่อแนบหลักฐานการบริจาค
   - รองรับไฟล์: PNG, JPG, PDF
   - ขนาดสูงสุด: 10MB
5. คลิก "ส่งข้อมูลการบริจาค"
6. รอ Admin ตรวจสอบและอนุมัติ
7. เมื่อได้รับการอนุมัติ จะได้รับอีเมลพร้อมใบเสร็จ PDF

---

## 🔍 ตรวจสอบว่าระบบทำงานถูกต้อง

### ทดสอบการเชื่อมต่อ Supabase
1. เปิด Supabase Dashboard
2. ไปที่ **Table Editor**
3. ตรวจสอบว่ามีตาราง `donations` และ `email_settings`
4. ลอง INSERT ข้อมูลทดสอบ

### ทดสอบการอัพโหลดไฟล์
1. ไปที่ **Storage** > `donations`
2. ลองอัพโหลดไฟล์ทดสอบ
3. ตรวจสอบว่าสามารถเข้าถึงไฟล์ผ่าน URL ได้

### ทดสอบการส่งอีเมล
1. ทดสอบส่งอีเมลโดยสร้างการบริจาคทดสอบ
2. ใช้อีเมลของคุณเอง
3. อนุมัติการบริจาค
4. ตรวจสอบกล่องจดหมายว่าได้รับอีเมลหรือไม่
5. หากไม่ได้รับ ตรวจสอบ Spam/Junk folder

---

## ❗ แก้ปัญหาที่พบบ่อย

### ปัญหา: "Error connecting to Supabase"
**แก้ไข:**
1. ตรวจสอบไฟล์ `.env.local`
2. ตรวจสอบว่าคัดลอก API keys ถูกต้อง
3. Restart development server (`Ctrl+C` แล้ว `npm run dev` ใหม่)

### ปัญหา: "Cannot upload file"
**แก้ไข:**
1. ตรวจสอบว่าสร้าง bucket `donations` แล้ว
2. ตรวจสอบว่าตั้งเป็น Public bucket
3. ตรวจสอบ Storage Policies

### ปัญหา: "Email not sent"
**แก้ไข:**
1. ตรวจสอบการตั้งค่า Email ใน Admin Dashboard
2. ตรวจสอบว่าใช้ App Password ไม่ใช่ password ปกติ
3. ตรวจสอบว่าเปิด 2-Step Verification แล้ว
4. ลอง port อื่น: 465 (SSL) แทน 587 (TLS)

### ปัญหา: "Table doesn't exist"
**แก้ไข:**
1. ตรวจสอบว่ารัน SQL script ครบทั้งหมด
2. ไปที่ SQL Editor และรันใหม่อีกครั้ง
3. ตรวจสอบว่าไม่มี error ในการรัน SQL

### ปัญหา: "Permission denied"
**แก้ไข:**
1. ตรวจสอบ RLS Policies
2. ตรวจสอบว่ารัน SQL policies ครบถ้วน
3. ลองปิด RLS ชั่วคราวเพื่อทดสอบ (ไม่แนะนำในการใช้งานจริง)

---

## 🚀 Deploy to Production

### Vercel (แนะนำ)
1. Push code ไป GitHub
2. ไปที่ https://vercel.com
3. Import โปรเจค
4. เพิ่ม Environment Variables ใน Settings
5. Deploy

### Environment Variables สำหรับ Production
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Console Log ใน Browser (F12)
2. ตรวจสอบ Terminal Log ของ Next.js server
3. อ่าน Documentation ใน README.md
4. ตรวจสอบ Supabase Logs ใน Dashboard

---

**สำเร็จ!** ระบบพร้อมใช้งานแล้ว 🎉
